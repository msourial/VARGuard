# Server-only TxLINE credential. Activate the World Cup free tier first.
TXLINE_API_ORIGIN=https://txline.txodds.com
TXLINE_API_TOKEN=
# Optional: an existing guest JWT. If omitted or expired, VARGuard requests one automatically.
TXLINE_JWT=
NEXT_PUBLIC_DEMO_MODE=true
